# blbl-android 项目文档

## 1. 项目概述

`blbl-android` 是一个第三方哔哩哔哩安卓应用，专为大屏设备（平板、TV、车机）设计，支持触摸和遥控操作，兼容 Android 5.0+。

### 1.1 核心特点

| 特性 | 说明 |
|------|------|
| **多设备支持** | 适配平板、TV、车机等大屏设备 |
| **双操作模式** | 支持触摸和遥控（DPAD）操作 |
| **低版本兼容** | 最低支持 Android 5.0 (API 21) |
| **播放器可选** | 支持 Media3(ExoPlayer) 和 IjkPlayer |

### 1.2 功能概览

- 侧边栏导航：搜索 / 推荐 / 分类 / 动态 / 直播 / 我的
- 扫码登录入口
- 视频播放：分辨率/编码/倍速/字幕/弹幕等设置
- 直播观看与弹幕
- 个人中心：收藏、历史、追番、稍后再看
- 设置页：播放与弹幕偏好等

---

## 2. 技术架构

### 2.1 技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 语言 | Kotlin | 1.9.x |
| 框架 | AndroidX | - |
| 视图绑定 | ViewBinding | - |
| 播放器 | Media3 (ExoPlayer) | 1.8.0 |
| 播放器 | IjkPlayer | 自定义编译 |
| 网络 | OkHttp | 4.12.0 |
| 序列化 | Protobuf-lite | 3.25.5 |
| UI组件 | Material Design | 1.12.0 |
| 布局 | RecyclerView / ViewPager2 | - |

### 2.2 模块架构

```
app/
├── src/main/java/blbl/cat3399/
│   ├── core/           # 核心基础模块
│   │   ├── account/    # 账户会话管理
│   │   ├── api/        # API接口层
│   │   ├── emote/      # 表情系统
│   │   ├── image/      # 图片加载
│   │   ├── io/         # 文件操作
│   │   ├── log/        # 日志与崩溃追踪
│   │   ├── model/      # 数据模型
│   │   ├── net/        # 网络层
│   │   ├── note/       # 笔记相关
│   │   ├── paging/     # 分页状态管理
│   │   ├── prefs/      # 偏好设置
│   │   ├── theme/      # 主题管理
│   │   ├── tv/         # TV遥控支持
│   │   ├── ui/         # UI组件基础
│   │   ├── update/     # 更新机制
│   │   └── util/       # 工具类
│   ├── feature/        # 功能业务模块
│   │   ├── category/   # 分类页
│   │   ├── custom/     # 自定义页面
│   │   ├── dynamic/    # 动态页
│   │   ├── following/  # 关注列表
│   │   ├── home/       # 首页推荐
│   │   ├── live/       # 直播模块
│   │   ├── login/      # 登录模块
│   │   ├── my/         # 个人中心
│   │   ├── player/     # 播放器模块
│   │   ├── risk/       # 风控验证
│   │   └── search/     # 搜索模块
│   ├── ui/             # 主界面组件
│   └── BlblApp.kt      # 应用入口
```

---

## 3. 核心模块详解

### 3.1 网络层 (core/net)

#### 3.1.1 BiliClient

**职责**：封装网络请求，管理 Cookie，处理 WBI 签名

**关键方法**：

| 方法 | 功能 |
|------|------|
| `init(context)` | 初始化网络客户端 |
| `requestString(url, method, headers)` | 发起字符串请求 |
| `getJson(url, headers)` | 获取 JSON 响应 |
| `postFormJson(url, form, headers)` | POST 表单请求 |
| `ensureWbiKeys()` | 获取 WBI 签名密钥 |
| `signedWbiUrl(path, params, keys)` | 生成带 WBI 签名的 URL |

**多客户端设计**：
- `apiOkHttp`：API 请求（带 Cookie）
- `apiOkHttpNoCookies`：API 请求（无 Cookie）
- `cdnOkHttp`：CDN 请求（视频/图片）
- `appCdnOkHttp`：应用更新 CDN

### 3.2 API 层 (core/api)

#### 3.2.1 BiliApi（主 API 入口）

**核心 API 方法**：

| 分类 | 方法 | 功能 |
|------|------|------|
| 导航 | `nav()` | 获取登录状态和用户信息 |
| 搜索 | `searchVideo(keyword, page)` | 搜索视频 |
| 搜索 | `searchLiveRoom(keyword, page)` | 搜索直播间 |
| 关系 | `relationStat(vmid)` | 获取关注/粉丝数 |
| 历史 | `historyCursor(max, business, viewAt)` | 获取历史记录 |
| 收藏 | `favFolders(upMid)` | 获取收藏夹列表 |
| 追番 | `bangumiFollowList(vmid, type)` | 获取追番列表 |
| 视频 | `videoDetail(bvid)` | 获取视频详情 |
| 弹幕 | `dmSeg(cid, segmentIndex)` | 获取弹幕分段 |

#### 3.2.2 VideoApi

**职责**：视频相关 API，包括播放、弹幕、互动等

**关键方法**：
- `dmSeg(cid, segmentIndex)` - 获取弹幕数据
- `dmWebView(cid, aid)` - 获取弹幕设置
- `archiveLike(bvid, like)` - 点赞视频
- `coinAdd(bvid, multiply)` - 投币
- `favResourceDeal(rid, addMediaIds, delMediaIds)` - 收藏操作

#### 3.2.3 LiveApi

**职责**：直播相关 API

#### 3.2.4 SearchApi

**职责**：搜索相关 API

### 3.3 数据模型 (core/model)

#### 3.3.1 VideoCard

视频卡片数据模型：

| 字段 | 类型 | 说明 |
|------|------|------|
| `bvid` | String | 视频 BVID |
| `cid` | Long? | 视频分P ID |
| `aid` | Long? | 视频 AID |
| `title` | String | 视频标题 |
| `coverUrl` | String | 封面 URL |
| `durationSec` | Int | 时长（秒） |
| `ownerName` | String | UP 主名称 |
| `ownerMid` | Long? | UP 主 ID |
| `view` | Long? | 播放量 |
| `danmaku` | Long? | 弹幕数 |
| `pubDate` | Long? | 发布时间 |

#### 3.3.2 Danmaku

弹幕数据模型：

| 字段 | 类型 | 说明 |
|------|------|------|
| `timeMs` | Long | 出现时间（毫秒） |
| `mode` | Int | 弹幕模式（滚动/顶部/底部等） |
| `text` | String | 弹幕内容 |
| `color` | Int | 颜色（ARGB） |
| `fontSize` | Int | 字体大小 |
| `midHash` | String? | 用户 ID 哈希 |
| `dmid` | Long? | 弹幕 ID |

#### 3.3.3 其他模型

- `BangumiSeason` - 番剧季信息
- `BangumiEpisode` - 番剧集信息
- `LiveRoomCard` - 直播间卡片
- `FavFolder` - 收藏夹
- `HistoryEntry` - 历史记录项
- `Following` - 关注用户

### 3.4 UI 组件 (core/ui)

#### 3.4.1 BaseActivity

所有 Activity 的基类，提供：
- 用户缩放支持
- 主题颜色管理
- 焦点返回机制

#### 3.4.2 焦点管理组件

| 组件 | 功能 |
|------|------|
| `FocusReturn` | 焦点返回管理 |
| `FocusTreeUtils` | 焦点树工具 |
| `DpadGridController` | DPAD 网格导航控制 |
| `TabLayoutFocus` | TabLayout 焦点管理 |

#### 3.4.3 弹窗组件

| 组件 | 功能 |
|------|------|
| `AppPopup` | 统一弹窗封装 |
| `PopupHandle` | 弹窗句柄管理 |
| `PopupHost` | 弹窗宿主 |

### 3.5 播放器模块 (feature/player)

#### 3.5.1 播放器引擎

支持两种播放器引擎：

| 引擎 | 特点 |
|------|------|
| **ExoPlayer** | Media3 官方播放器，默认选择 |
| **IjkPlayer** | 自定义编译，支持更多编码格式 |

**切换方式**：通过 `AppPrefs.playerEngineKind` 设置

#### 3.5.2 弹幕系统

**弹幕引擎组件**：
- `DanmakuEngine` - 弹幕解析与管理
- `DanmakuPlayer` - 弹幕渲染控制
- `DanmakuView` - 弹幕显示视图
- `DanmakuTimer` - 弹幕时间同步

**弹幕过滤**：
- `DanmakuShield` - 关键词过滤
- `DanmakuUserFilter` - 用户自定义过滤规则

#### 3.5.3 PlayerActivity

视频播放主界面，包含：
- 播放控制（播放/暂停/快进/快退）
- 弹幕设置（开关/速度/透明度）
- 画质选择（分辨率/编码）
- 倍速播放（0.5x ~ 2.0x）
- 字幕支持
- 评论区入口
- 推荐视频列表

---

## 4. 主界面架构

### 4.1 MainActivity

**职责**：主界面容器，管理侧边栏导航和内容区域

**核心功能**：
- 侧边栏导航（推荐/分类/动态/直播/我的）
- 用户信息展示与切换
- 焦点管理（TV 模式）
- 自动更新检查
- IjkPlayer 内核更新提示

**布局结构**：
```
┌─────────────────────────────────────────────────┐
│  Sidebar (侧边栏)          │  Main Container   │
│  ┌─────────────────────┐   │  (内容区域)       │
│  │  用户头像/登录按钮   │   │                  │
│  ├─────────────────────┤   │                  │
│  │  导航列表           │   │                  │
│  │  - 推荐             │   │                  │
│  │  - 分类             │   │   Fragment       │
│  │  - 动态             │   │   (Home/         │
│  │  - 直播             │   │   Category/      │
│  │  - 我的             │   │   Dynamic/       │
│  ├─────────────────────┤   │   Live/          │
│  │  设置按钮           │   │   My)            │
│  └─────────────────────┘   │                  │
└─────────────────────────────┴─────────────────┘
```

### 4.2 导航注册

**MainRootNavRegistry**：注册所有根导航项

**导航项 ID**：
- `ID_HOME` - 首页推荐
- `ID_CATEGORY` - 分类页
- `ID_DYNAMIC` - 动态页
- `ID_LIVE` - 直播页
- `ID_MY` - 个人中心

---

## 5. 功能模块详解

### 5.1 首页推荐 (feature/home)

**组件**：
- `HomeFragment` - 首页容器
- `HomePagerAdapter` - 分页适配器
- `PgcRecommendGridFragment` - PGC 推荐网格

**功能**：
- 推荐视频流
- PGC 内容推荐（番剧/电影）

### 5.2 分类页 (feature/category)

**组件**：
- `CategoryFragment` - 分类容器
- `CategoryPagerAdapter` - 分区分页
- `CategoryZones` - 分区定义

**分区类型**：
- 动画、番剧、国创
- 音乐、舞蹈、游戏
- 科技、数码、生活等

### 5.3 动态页 (feature/dynamic)

**组件**：
- `DynamicFragment` - 动态容器
- `FollowingAdapter` - 关注列表适配器

**功能**：
- 关注 UP 主动态流
- 视频动态预览

### 5.4 直播页 (feature/live)

**组件**：
- `LiveFragment` - 直播容器
- `LiveGridFragment` - 直播网格
- `LiveAreaIndexFragment` - 分区入口
- `LivePlayerActivity` - 直播播放页

**功能**：
- 推荐直播
- 直播分区浏览
- 直播弹幕互动

### 5.5 个人中心 (feature/my)

**组件**：
- `MyFragment` - 个人中心容器
- `MyPagerAdapter` - 分页适配器
- 子模块：
  - `MyHistoryFragment` - 历史记录
  - `MyFavFoldersFragment` - 收藏夹
  - `MyBangumiFollowFragment` - 追番列表
  - `MyLikeFragment` - 点赞视频
  - `MyToViewFragment` - 稍后再看

### 5.6 搜索模块 (feature/search)

**组件**：
- `SearchFragment` - 搜索界面
- `SearchAdapters` - 搜索结果适配器

**功能**：
- 热门搜索词
- 搜索建议
- 多类型搜索（视频/番剧/直播/用户）

### 5.7 登录模块 (feature/login)

**组件**：
- `QrLoginActivity` - 扫码登录

**登录流程**：
1. 获取二维码
2. 用户扫码确认
3. 获取 Cookie
4. 保存会话

---

## 6. 数据存储

### 6.1 Cookie 管理

**CookieStore**：本地 Cookie 持久化

**关键方法**：
- `getCookieValue(name)` - 获取 Cookie 值
- `hasSessData()` - 检查是否已登录
- `loadCookies()` - 加载 Cookie
- `saveCookies()` - 保存 Cookie

### 6.2 账户会话

**AccountSessionStore**：多账户管理

**功能**：
- 保存多个账户会话
- 账户切换
- 账户信息存储

### 6.3 偏好设置

**AppPrefs**：应用设置管理

**主要设置项**：
| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| `userAgent` | 用户代理 | 移动端 UA |
| `fullscreenEnabled` | 全屏模式 | true |
| `playerEngineKind` | 播放器引擎 | ExoPlayer |
| `autoUpdateCheckEnabled` | 自动更新检查 | true |
| `disclaimerAccepted` | 免责声明已接受 | false |

---

## 7. 构建与部署

### 7.1 环境要求

| 依赖 | 版本 |
|------|------|
| JDK | 17 |
| Android SDK | compileSdk 36 |
| Gradle | 8.5+ |

### 7.2 构建命令

**调试包**：
```bash
./gradlew assembleDebug
```

**发布包**：
```bash
./gradlew assembleRelease
```

**自定义版本**：
```bash
./gradlew assembleRelease -PversionName=0.1.1 -PversionCode=2
```

### 7.3 GitHub Actions

**工作流**：
- `android-debug.yml` - 调试包构建
- `android-release.yml` - 发布包构建

**Secrets 配置**：
- `RELEASE_KEYSTORE_BASE64` - 签名文件
- `RELEASE_STORE_PASSWORD` - 存储密码
- `RELEASE_KEY_ALIAS` - 密钥别名
- `RELEASE_KEY_PASSWORD` - 密钥密码

---

## 8. 安全与隐私

### 8.1 免责声明

应用启动时展示免责声明，用户必须接受才能使用：
- 不得用于非法活动
- 不得干扰 B 站正常运营
- 禁止官方平台宣传
- 禁止牟利

### 8.2 Crash 追踪

**CrashTracker**：
- 捕获未处理异常
- 本地存储 crash 日志
- 提示用户上传日志

### 8.3 日志管理

**AppLog**：
- 分级日志（DEBUG/INFO/WARN/ERROR）
- 日志导出功能
- 日志上传接口

---

## 9. 二次开发指南

### 9.1 新增功能流程

1. **需求分析**：明确功能需求和边界
2. **模块划分**：确定所属模块（feature/xxx）
3. **API 对接**：在 `core/api` 添加接口
4. **数据模型**：在 `core/model` 定义数据类
5. **UI 实现**：创建 Fragment/Activity
6. **导航注册**：在 `MainRootNavRegistry` 注册（如需）
7. **测试验证**：确保功能正常

### 9.2 常用扩展点

| 扩展点 | 位置 | 说明 |
|--------|------|------|
| 新导航项 | `MainRootNavRegistry` | 添加新的侧边栏导航 |
| 新 API | `core/api` | 添加新接口 |
| 新数据模型 | `core/model` | 定义新数据类 |
| 新 Fragment | `feature/xxx` | 添加新功能页面 |
| 设置项 | `core/prefs/AppPrefs` | 添加新配置项 |

### 9.3 注意事项

1. **线程安全**：网络请求使用 Coroutine，注意线程切换
2. **焦点管理**：TV 模式下需处理 DPAD 焦点
3. **资源清理**：Activity/Fragment 生命周期管理
4. **异常处理**：API 调用需处理异常情况
5. **Cookie 同步**：登录状态变化需同步 Cookie

---

## 10. 附录

### 10.1 API 参考

**主要 API 端点**：
- `https://api.bilibili.com/x/web-interface/nav` - 导航信息
- `https://api.bilibili.com/x/web-interface/search/all` - 搜索
- `https://api.bilibili.com/x/v2/history/cursor` - 历史记录
- `https://api.bilibili.com/x/v3/fav/folder/info` - 收藏夹

### 10.2 第三方参考

| 项目 | 说明 |
|------|------|
| [bilibili-API-collect](https://github.com/SocialSisterYi/bilibili-API-collect) | B 站 API 文档 |
| [BBLL](https://github.com/xiaye13579/BBLL) | 页面设计参考 |
| [PiliPlus](https://github.com/bggRGjQaUbCoE/PiliPlus) | 功能逻辑参考 |
| [ijkplayer](https://github.com/cat3399/ijkplayer) | 播放器内核 |

---

**文档版本**：v1.0  
**生成时间**：2026-05-17  
**项目版本**：0.1.0

# 二次开发

---

## 功能一：PGC 分类筛选功能

### 1.1 需求概述

**目标**：为应用添加类似正规影视APP的细致分类功能，支持按地区、类型、语言、年份、排序等条件筛选电影/电视剧/番剧/纪录片/国创/综艺等PGC内容。

**现状分析**：
- 现有分类仅有UGC分区（动画、音乐、游戏等），是用户上传内容
- 首页虽有"番剧"和"影视"Tab，但只是简单的推荐流，无分类筛选

**目标状态**：
- 新增 PGC 分类页面，支持多维度筛选
- 使用 B站官方 `/pgc/season/index/result` API

### 1.2 API 信息

**接口地址**：`https://api.bilibili.com/pgc/season/index/result`

**请求方式**：GET

**鉴权方式**：Cookie（SESSDATA），部分内容需要登录

### 1.3 内容类型（season_type）

| season_type | 内容类型 | 说明 |
|-------------|----------|------|
| 1 | 番剧 | 日本动画 |
| 2 | 电影 | 电影资源 |
| 3 | 纪录片 | 纪录片 |
| 4 | 国创 | 国产动画 |
| 5 | 电视剧 | 电视剧 |
| 7 | 综艺 | 综艺节目 |

### 1.4 筛选参数

| 参数名 | 类型 | 必要性 | 说明 | 可选值示例 |
|--------|------|--------|------|-----------|
| `season_type` | num | 必填 | 内容类型 | 1-7（见上表） |
| `type` | num | 必填 | 固定值 | `1` |
| `page` | num | 可选 | 页码 | 默认 1 |
| `pagesize` | num | 可选 | 每页数量 | 默认 20 |
| `area` | str | 可选 | 地区 | `-1`=全部, `1`=中国, `2`=日本, `3`=美国 |
| `style_id` | num | 可选 | 风格/题材 | `-1`=全部, 其他值需抓包获取 |
| `spoken_language_type` | num | 可选 | 配音语言 | `-1`=全部, `1`=原声, `2`=中文配音 |
| `season_version` | num | 可选 | 类型 | `-1`=全部, `1`=正片, `2`=电影 |
| `is_finish` | num | 可选 | 连载状态 | `-1`=全部, `1`=完结, `0`=连载 |
| `season_status` | num | 可选 | 付费类型 | `-1`=全部, `1`=免费, `4`=大会员 |
| `year` | str | 可选 | 年份（番剧/国创） | `[2025,2026)` 表示2025年 |
| `release_date` | str | 可选 | 上映年份（电影/电视剧） | 格式同 year |
| `order` | num | 可选 | 排序方式 | `0`=更新时间, `2`=播放量, `4`=评分, `6`=上映时间 |
| `sort` | num | 可选 | 排序方向 | `0`=降序, `1`=升序 |

### 1.5 地区代码表

| ID | 地区 |
|----|------|
| -1 | 全部 |
| 1 | 中国大陆 |
| 2 | 日本 |
| 3 | 美国 |
| 4 | 英国 |
| 6 | 中国香港 |
| 7 | 中国台湾 |
| 8 | 韩国 |

### 1.6 UI 设计建议

#### 1.6.1 页面结构

```
┌────────────────────────────────────────────┐
│  [返回]  影视分类                    [筛选] │
├────────────────────────────────────────────┤
│  番剧 | 电影 | 电视剧 | 纪录片 | 国创 | 综艺 │  ← 一级 Tab
├────────────────────────────────────────────┤
│  [全部地区▼] [全部题材▼] [全部状态▼] [排序▼] │  ← 快捷筛选栏
├────────────────────────────────────────────┤
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐              │
│  │    │ │    │ │    │ │    │              │
│  │封面│ │封面│ │封面│ │封面│              │
│  │    │ │    │ │    │ │    │              │
│  └────┘ └────┘ └────┘ └────┘              │
│  标题    标题    标题    标题               │  ← 网格列表
│  9.8分   9.5分   8.9分   9.1分            │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐              │
│  ...                                   ▼  │
└────────────────────────────────────────────┘
```

#### 1.6.2 各分类筛选项

**番剧/国创筛选项**：
- 地区（全部/中国/日本/其他）
- 题材（全部/热血/恋爱/搞笑/校园/奇幻/日常/科幻/冒险/悬疑/其他）
- 季度（全部/一月/四月/七月/十月）
- 年份（全部/2025/2024/2023/更早）
- 状态（全部/完结/连载）
- 排序（更新时间/追番人数/评分/开播时间）

**电影筛选项**：
- 地区（全部/中国/日本/美国/韩国/其他）
- 题材（全部/动作/喜剧/爱情/科幻/动画/悬疑/惊悚/犯罪/战争/其他）
- 年份（上映时间）
- 付费状态（全部/免费/付费/大会员）
- 排序（上映时间/播放量/评分）

**电视剧筛选项**：
- 地区（全部/中国/美国/韩国/其他）
- 题材（全部/都市/农村/悬疑/古装/青春/涉案/喜剧/军旅/武侠/其他）
- 年份
- 状态
- 排序

**纪录片筛选项**：
- 地区
- 题材（全部/科学/自然/历史/社会/人文/旅行/美食/动物）
- 年份
- 排序

**综艺筛选项**：
- 地区
- 题材（全部/真人秀/选秀/访谈/音乐/美食/旅行/情感/其他）
- 年份
- 排序

### 1.7 实现步骤

| 步骤 | 内容 | 涉及文件 |
|------|------|----------|
| 1 | 新增 `PgcCategoryApi` 封装分类API | `core/api/PgcCategoryApi.kt` |
| 2 | 新增 `PgcSeason` 数据模型 | `core/model/PgcSeason.kt` |
| 3 | 新增 `PgcCategoryFragment` 分类页面 | `feature/category/PgcCategoryFragment.kt` |
| 4 | 新增 `PgcCategoryAdapter` 列表适配器 | `feature/category/PgcCategoryAdapter.kt` |
| 5 | 新增筛选弹窗 UI 和逻辑 | `feature/category/PgcFilterDialog.kt` |
| 6 | 在导航中注册新页面 | `MainActivity` / `CustomPageTabRegistry` |
| 7 | 添加筛选栏 UI | `layout/fragment_pgc_category.xml` |
| 8 | 添加字符串资源 | `res/values/strings.xml` |

### 1.8 数据模型

```kotlin
data class PgcSeason(
    val seasonId: Long,
    val mediaId: Long,
    val title: String,
    val cover: String,
    val areas: List<String>,        // 地区
    val styles: List<String>,      // 风格/题材
    val typeName: String,          // 类型名称
    val seasonTypeName: String?,   // 子类型
    val newEp: NewEpInfo?,         // 最新集信息
    val rating: RatingInfo?,       // 评分信息
    val isFinish: Boolean?,        // 是否完结
    val badge: String?,            // 角标（大会员/独家/限免等）
    val view: Long?,               // 播放量
    val follow: Long?,             // 追番/追剧人数
    val danmaku: Long?,            // 弹幕数
    val publishDate: String?,      // 上映/发布时间
)

data class RatingInfo(
    val score: Double,
    val count: Long,
)
```

### 1.9 验收标准

| 序号 | 验收项 | 状态 |
|------|--------|------|
| 1 | 番剧/电影/电视剧/纪录片/国创/综艺六个一级分类Tab可切换 | ✅ |
| 2 | 每个分类支持地区筛选（至少支持：全部/中国/日本/其他） | ✅ |
| 3 | 每个分类支持排序切换（更新时间/播放量/评分/追番人数） | ✅ |
| 4 | 番剧支持年份/季度筛选 | ✅ |
| 5 | 电影支持上映年份筛选 | ✅ |
| 6 | 下拉刷新和上拉加载更多正常 | ✅ |
| 7 | 点击卡片可进入详情页 | ✅ |
| 8 | 筛选条件变化后自动刷新列表 | ✅ |
| 9 | 筛选弹窗操作流畅 | ✅ |

### 1.10 注意事项

1. **API 限制**：该接口需要 SESSDATA 才能获取完整数据，未登录可能只能获取部分内容
2. **分页加载**：使用 `page` 和 `pagesize` 参数实现分页
3. **缓存策略**：热门分类结果可本地缓存，避免频繁请求
4. **题材ID**：`style_id` 的具体值需要抓包获取，开发时动态获取
5. **DPI适配**：网格列数应根据屏幕宽度和DPI动态计算

---

**开发状态**：待确认
